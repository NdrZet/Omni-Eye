<Window x:Class="OmniEyeTray.Views.CompanionDiscoveryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Comp_WindowTitle}" 
        Height="540" Width="860"
        MinHeight="460" MinWidth="720"
        WindowStartupLocation="CenterOwner"
        Background="#202020"
        Foreground="#FFFFFF"
        FontFamily="Segoe UI Variable, Segoe UI, -apple-system, sans-serif">

    <Window.Resources>
        <Style x:Key="FluentButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#383838"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="#454545"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#252525"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FluentPrimaryButton" TargetType="Button" BasedOn="{StaticResource FluentButton}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="BorderBrush" Value="#1B87DE"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="BtnBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#1886D9"/>
                                <Setter TargetName="BtnBorder" Property="BorderBrush" Value="#2B95E8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="BtnBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#2B2B2B" CornerRadius="8" Width="40" Height="40" Margin="0,0,12,0" 
                        BorderBrush="#383838" BorderThickness="1">
                    <TextBlock Text="🧩" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource Comp_Header}" FontWeight="Bold" FontSize="15" Foreground="#FFFFFF" />
                    <TextBlock x:Name="TxtSubtitle" Text="{DynamicResource Comp_Subtitle}" 
                               FontSize="11" Foreground="#A0A0A0" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Quick selection bar -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TxtSummary" Text="{DynamicResource Comp_Instruction}" 
                       FontSize="12" Foreground="#A0A0A0" VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="{DynamicResource Comp_SelectAll}" Style="{StaticResource FluentButton}" Padding="10,4" Margin="0,0,6,0" Click="BtnSelectAll_Click"/>
                <Button Content="{DynamicResource Comp_UnselectAll}" Style="{StaticResource FluentButton}" Padding="10,4" Click="BtnUnselectAll_Click"/>
            </StackPanel>
        </Grid>

        <!-- Components DataGrid -->
        <Border Grid.Row="2" Background="#2B2B2B" CornerRadius="8" BorderBrush="#383838" BorderThickness="1" Margin="0,0,0,14">
            <DataGrid x:Name="DgComponents" AutoGenerateColumns="False" 
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      Background="Transparent" RowBackground="#2B2B2B" 
                      AlternatingRowBackground="#262626"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333333"
                      HeadersVisibility="Column" BorderThickness="0"
                      Foreground="#FFFFFF">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#202020" />
                        <Setter Property="Foreground" Value="#A0A0A0" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="FontSize" Value="11" />
                        <Setter Property="Padding" Value="10,8" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="BorderBrush" Value="#383838" />
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="8,6" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <!-- Checkbox Column -->
                    <DataGridTemplateColumn Width="46">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- File Name -->
                    <DataGridTextColumn Header="{DynamicResource Comp_ColFile}" Binding="{Binding FileName}" Width="170" FontWeight="SemiBold"/>

                    <!-- Role Badge -->
                    <DataGridTemplateColumn Header="{DynamicResource Comp_ColRole}" Width="170">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#1C2738" BorderBrush="#254168" BorderThickness="1" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Role}" FontSize="10" FontWeight="SemiBold" Foreground="#4CC2FF"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Signature -->
                    <DataGridTemplateColumn Header="{DynamicResource Comp_ColSignature}" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Background="#133821" BorderBrush="#1E6135" BorderThickness="1" CornerRadius="10" Padding="8,2" 
                                            Visibility="{Binding IsSigned, Converter={x:Null}}">
                                        <TextBlock Text="{DynamicResource App_Signed}" FontSize="10" FontWeight="SemiBold" Foreground="#6CCB5F"/>
                                    </Border>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Full Path -->
                    <DataGridTextColumn Header="{DynamicResource Comp_ColPath}" Binding="{Binding FilePath}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer Actions -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TxtSelectionCount" Text="{DynamicResource Comp_SelectionFormat}" 
                       FontSize="12" Foreground="#A0A0A0" VerticalAlignment="Center" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="BtnCancel" Content="{DynamicResource Comp_Cancel}" 
                        Style="{StaticResource FluentButton}" Margin="0,0,8,0" Click="BtnCancel_Click"/>
                <Button x:Name="BtnConfirm" Content="{DynamicResource Comp_Confirm}" 
                        Style="{StaticResource FluentPrimaryButton}" Click="BtnConfirm_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
